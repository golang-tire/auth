export const configs = {
    API_URL: "http://localhost:8080/v1",
    BEARER_PARAM: "Bearer",
    PAGE_SIZE: 10,
}

export const URLS = {
    USERS: configs.API_URL + "/users",
    DOMAINS: configs.API_URL + "/domains",
    ROLES: configs.API_URL + "/roles",
}